/**
 * Tipos do módulo countries (tb_country na setes_central).
 * Espelho no app: apps/web/lib/app/modules/countries/domain/entity/country_entity.dart
 */

export interface CountryRow {
  id:   number
  name: string | null
}
